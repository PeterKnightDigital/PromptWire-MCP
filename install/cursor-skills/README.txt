PromptWire — personal Cursor skill (all projects)

Copy the promptwire skill into your personal skills folder so every ProcessWire
workspace gets PromptWire workflow guidance even without a project rule:

  cp -R install/cursor-skills/promptwire ~/.cursor/skills/promptwire

For per-project rules (stronger — applies every chat in that repo), use
install/cursor-rules/ instead.
